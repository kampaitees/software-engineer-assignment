'use client'
import { useRef,useState } from 'react'; import {useRouter} from 'next/navigation'; import Nav from '../../../components/Nav'; import {api} from '../../../lib/api'
export default function NewKit(){const [jd,setJd]=useState('');const [company,setCompany]=useState('');const [days,setDays]=useState(5);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const fileRef=useRef<HTMLInputElement>(null);const r=useRouter();
async function submit(){setBusy(true);setError('');try{const out=await api<any>('/kits',{method:'POST',body:JSON.stringify({jd,company_url:company,days})});r.push(`/kits/${out.kitId||out.kit._id}`)}catch(e){setError(e instanceof Error?e.message:'Failed')}finally{setBusy(false)}}
async function upload(f:File){try{const parsed=JSON.parse(await f.text());if(!Array.isArray(parsed)||!parsed[0]) throw new Error();setJd(parsed[0].jd||'');setCompany(parsed[0].company_url||'');setDays(parsed[0].days||5)}catch{setError('Batch file must be valid Appendix B JSON.')}}
return <><Nav/><main className="max-w-4xl mx-auto p-6"><div className="card p-6 space-y-5"><div><h1 className="text-3xl font-black">Create a prep kit</h1><p className="text-gray-500 mt-1">Research → targeted generation → coverage loop → deterministic schedule.</p></div><label className="block"><span className="text-sm font-bold">Job description</span><textarea className="input mt-2 min-h-72" value={jd} onChange={e=>setJd(e.target.value)} placeholder="Paste the complete job description…"/></label><label className="block"><span className="text-sm font-bold">Company website</span><input className="input mt-2" value={company} onChange={e=>setCompany(e.target.value)} placeholder="https://example.com"/></label><label className="block"><span className="text-sm font-bold">Days before interview</span><input className="input mt-2" type="number" min={1} max={60} value={days} onChange={e=>setDays(Number(e.target.value))}/></label><div className="flex flex-wrap gap-3 items-center"><input ref={fileRef} type="file" accept="application/json" hidden onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/><button className="btn btn-secondary" onClick={()=>fileRef.current?.click()}>Load first case from JSON</button><button disabled={busy} className="btn btn-primary" onClick={submit}>{busy?'Starting pipeline…':'Generate kit'}</button></div>{error&&<div className="p-3 bg-red-50 text-red-700 rounded-lg text-sm">{error}</div>}</div></main></>}
